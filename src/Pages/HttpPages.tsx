class HttpPages {
    Profile: string = "/Profile"
    Orders: string = "/Orders"
    Auth: string = "/Auth"
    NotFound: string = "*";
    Products: string = '/Products'
}

export default new HttpPages();